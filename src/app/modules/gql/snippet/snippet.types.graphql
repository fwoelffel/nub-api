type Query {
  getSnippets: [Snippet]
}

type Mutation {
  createSnippet(name: String!, description: String, content: String!): Snippet
}

type Snippet {
  name: String!
  description: String
  content: String!
  createdDate: String
  updatedDate: String
  id: ID!
  mime: String
}
